=========
twowaymap
=========

This is a python module that implements a 2-way mapping type - like a ``dict``, but as well as looking up values by keys (``d[k] -> v``), you can look up keys by values (``d[v] -> k``). 

